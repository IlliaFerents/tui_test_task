# Playwright Environment Variables (Example)
BASE_URL=https://www.tui.nl

# Log generated test data to console (set to "false" to suppress)
LOG_TEST_DATA=true

# Enable debug-level log output
# DEBUG=true

# CI mode: enables headless, retries (2), single worker
# CI=true
